export interface CardapioModels {
    // Atributos de Cardápio
    carcodigo? : number;
    cardata : string;

    // Atributos de BaseModel
    create_at? : string;
    update_at? : string;
    active? : boolean;
}
